{ "remote-as": "{{ remote_asn }}", "description": "{{ remote_site }}.tun0", "ebgp-multihop": "4", "route-map": { "import": "{{ local_site }}in", "export": "{{ local_site }}out" }, "weight": "{{ tun0_bgp_weight }}", "metric": "{{ tun0_bgp_metric }}", "soft-reconfiguration": "inbound" }
