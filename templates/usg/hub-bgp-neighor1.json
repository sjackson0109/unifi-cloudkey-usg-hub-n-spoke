{ "remote-as": "{{ local_asn }}", "description": "{{ local_site }}.tun1", "ebgp-multihop": "4", "route-map": { "import": "{{ remote_site }}in", "export": "{{ remote_site }}out" }, "weight": "{{ tun1_bgp_weight }}", "metric": "{{ tun1_bgp_metric }}", "soft-reconfiguration": "inbound" }