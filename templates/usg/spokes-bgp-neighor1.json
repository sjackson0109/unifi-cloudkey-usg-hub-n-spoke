{ "remote-as": "{{ remote_asn }}", "description": "{{ remote_site }}.tun1", "ebgp-multihop": "4", "route-map": { "import": "{{ local_site }}in", "export": "{{ local_site }}out" }, "weight": "{{ tun1_bgp_weight }}", "metric": "{{ tun1_bgp_metric }}", "soft-reconfiguration": "inbound" }